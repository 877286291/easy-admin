package top.houyuji.code.generator.core.enums;

/**
 * 输出文件类型
 */
public enum OutputFile {
    entity,
    dto,
    vo,
    query,
    repository,
    service,
    serviceImpl,
    controller,
    parent,
}
